package com.capstone.users_service.Enum;

public enum Role {
    /**
     * USER enum for creating a user role.
     */
    USER,
    /**
     * USER enum for creating a user role.
     */
    OWNER
}
